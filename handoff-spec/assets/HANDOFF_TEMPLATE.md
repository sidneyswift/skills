# Handoff: <task name>

> **Status: [ FINISHED — no in-scope work remains | NOT FINISHED — see Handoff Status below ]**
> As of: <date>. Handed off from: <platform/tool, e.g. "Claude Code on Anthropic API">. Reason: <e.g. "ran out of usage credits", "switching platforms">.

## 1. Goal / Spec

*(Stable. Do not rewrite on later handoffs of this same task — carry it forward verbatim unless the goal itself genuinely changed.)*

### Objective
<What was asked, restated plainly. Include the requester's own words where useful, plus a plain-language restatement so there's no ambiguity.>

### Definition of done
- <Acceptance criterion 1>
- <Acceptance criterion 2>

### Constraints / non-negotiables
- <Tech stack, deadlines, compliance, style rules, explicit things NOT to do, etc. — omit this subsection if none.>

## 2. Handoff Status

*(Point-in-time snapshot. Rewrite fresh at every handoff — this is the part that goes stale.)*

### Done — in scope, complete
- <Item> — <commit/PR/file reference>

### Done — but out of scope
*(Work that happened but wasn't required. Say why, and whether the successor should keep or revert it. Delete this subsection if nothing qualifies — don't leave it as a token empty heading.)*
- <Item> — <why it happened / keep or revert>

### Remaining — required, in scope
- <Item>

### Remaining — nice to have, not required
*(Delete if none.)*
- <Item>

### In production now (live)
- <Item> — <link/version/deploy detail>

### Merged — not yet deployed/live
- <Item> — <PR link>

### Still in the works (unmerged / in progress / local only)
- <Item> — <branch name, draft PR link, or "uncommitted, local only">

### Known gotchas / landmines
*(Things that look right but aren't, workarounds in place, flaky tests, false starts already ruled out — so the successor doesn't rediscover them the hard way. Delete if none.)*
- <Item>

## 3. Pointers

- Repo(s): <path or URL>
- Branch(es): <name>
- Relevant PRs: <#links>
- Relevant issues: <#links>
- Key files touched: <paths>
