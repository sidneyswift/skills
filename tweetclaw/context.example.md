# Setup

This skill uses one local config file:

`~/.config/tweetclaw/.env`

Suggested defaults:

```bash
SETUP_COMPLETE=true
DEFAULT_READ_LIMIT=50
REQUIRE_APPROVAL_FOR_VISIBLE_ACTIONS=yes
```

Keep private setup values in the local OpenClaw plugin config, not in this repo or chat.
