---
name: {OS}-system-promote-skill
description: Promote proven {DOMAIN} work into a durable workspace skill. Use after a task succeeds, after repeated manual work, when a failure should not recur, or when the user says "skillify this", "make this repeatable", "save this as a skill", or "we will do this again".
---

# {DOMAIN} Skillify

Turn completed work into a reusable capability without cluttering the workspace. One-off output stays
with its source package; repeated or maintained work becomes a staged, verified skill in
`plugin/skills/`.

## Steps
1. **Confirm it should become a skill.** Ask: will this repeat, need upkeep, or prevent a failure from
   recurring? If no, keep the artifact with its source work package, record the durable-value
   disposition, and stop.
2. **Prove package provenance.** Identify the closed package, accepted result, source artifact, and
   evidence or failure that justifies the skill. Do not synthesize from vague memory.
3. **Name + triggers.** Name it `{OS}-{area}-{verb}-{noun}` (4 kebab words — the OS slug, the area it
   serves, then verb-noun; e.g. `{OS}-marketing-write-post`). Choose 3-5 trigger phrases a future user
   or agent would actually say.
4. **Stage first.** When a source package exists, draft in
   `work/{source-initiative}/YYYY-MM-DD-skillify-{name}/`. If no package exists and skillification
   itself crosses the managed-work threshold, create a package first. Never write a half-working
   skill directly into `plugin/skills/`.
5. **Extract the repeatable process.** Keep the invariant steps, decision rules, examples, and reusable
   assets. Remove false starts, chat fragments, and one-off facts.
6. **Separate judgment from exact work.** Put the workflow and decision rules in `SKILL.md`. Put
   mechanical or deterministic work in `scripts/`, `assets/`, or templates only when it removes real
   repetition.
7. **Verify before publishing.** Use the strongest cheap check for the domain: a script test, smoke
   command, sample prompt replay, fixture comparison, checklist dry run, or review rubric. Record what
   passed in the staged folder.
8. **Ask before publishing.** Show the proposed skill name, triggers, destination, and verification
   result. Move to `plugin/skills/{name}/` only after approval. **Unattended runs (e.g. a scheduled
   janitor, no human present):** do not publish — leave the verified draft in `work/` and log a proposal
   to `operations/improvements.md` for later approval. Publish to `plugin/skills/` only with explicit
   approval, or when the workspace declares an explicit autonomous-publish policy.
9. **Publish + repackage.** Move the staged skill into `plugin/skills/`, then re-zip `plugin/` to the
   delivery path (a transient artifact — never an `outputs/` folder inside the workspace) and update
   `operations/.packaged-version` so `{OS}-system-check-health` sees a fresh package. Update plugin README/index
   if present, and if the new skill should run unattended, add or update its prompt in `routines/`.
10. **Reconcile provenance.** Link the published skill and verification result from the source
    package. If it came from a failure or important decision, capture a reusable note in `knowledge/`
    with the same package evidence.

## Quality bar
- No skill without a real source result or failure.
- No half-working skill in `plugin/skills/`.
- No one-off deliverable promoted just because it was useful once.
- No domain facts invented by the agent; mark assumptions "draft — confirm".
